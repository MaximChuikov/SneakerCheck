import React, {FC} from 'react';
import {Layout} from "antd";

const Navbar: FC = () => {

    return (
        <Layout.Header>
            SneakersApp
        </Layout.Header>
    );
};

export default Navbar;
